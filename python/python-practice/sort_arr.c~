#include<stdio.h>

void main()
{

int n1,n2,i,j,arr1[20],arr2[20];

printf("Toatal no to be enter:");
scanf("%d",&n1);

printf("Enter the no for arr1:");

for(i=0;i<n1;i++)
	{
	scanf("%d",&arr1[i]);
	}
printf("Toatal no to be enter:");
scanf("%d",&n2);

printf("Enter the no for arr2:");

for(j=0;j<n2;i++)
	{
	scanf("%d",&arr2[j]);
	}

//sort_array(arr1,arr2,n);
//}

//void sort_array(int *arr1,int *arr2,int n)
//{
int k,temp[20];

for(i=0;i<n1;i++)
	{
	for(j=0;j<n2;j++)
	{
		if(arr1[i] < arr2[j])
		{
			temp[k]=arr1[i];
			i++;
		
	
		}
	else
		{
			temp[k]=arr2[j];
			j++;
		}

		k++;
	}

}
for(k=0;k<n1+n2;k++)
	{
	printf("%d",temp[k]);
	}




}
