#include<stdio.h>
void duplicate();

void duplicate(int arr[],int n)
{
	int i,j;
	for(i=0;i<n;i++)
	{
		for(j=i+1;j<n;j++)
		{
			if(arr[i]==arr[j])
			{	
				printf("Duplicate=%d",arr[i]);
				
			}
		}
	}	
}
main()
{
	int arr[]={1,2,2,3,4,5,5};
	duplicate(arr,7);
	printf("\n");
}
